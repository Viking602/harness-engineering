# AGENTS

This repo stores the `harness-engineering` skill, dispatch references, and custom agent definitions.

Start here:
- `docs/index.md` for the current docs map and entrypoints.
- `harness-engineering/SKILL.md` for the skill entrypoint and repo-shaping rules.
- `harness-engineering/references/subagent-dispatch-playbook.md` for parent-agent dispatch and escalation rules.
- `harness-engineering/codex/agents/` and `harness-engineering/agents/` for Codex and Claude agent definitions.

Routing rules:
- Keep this file short and routing-only.
- Update `AGENTS.md` and `docs/index.md` in the same pass when entrypoints, commands, or current docs change.
